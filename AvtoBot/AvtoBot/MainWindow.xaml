<Window x:Class="AvtoBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AvtoBot"
        mc:Ignorable="d"
        Background="#000"
        Title="Автоботы вперёд" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#32CD32"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E2F" />
            <Setter Property="Foreground" Value="#D3D3D3" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Background" Value="#1E1E2F" />
            <Setter Property="Foreground" Value="#FFFF00" />
        </Style>

        <Style TargetType="Grid">
            <Setter Property="Background" Value="#000" />
        </Style>

        <Style TargetType="Window">
            <Setter Property="Background" Value="#000" />
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.33*"/>
                <ColumnDefinition Width="0.33*"/>
                <ColumnDefinition Width="0.33*"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="InputExpression" Grid.Row="0" Grid.ColumnSpan="3" Margin="5" Padding="5" FontSize="16" />
            <Button Content="Вычислить" Grid.Row="1" Grid.Column="0" Margin="5" Padding="5" Click="Calculate"/>
            <Button Content="Добавить переменную" Grid.Row="1" Grid.Column="1" Margin="5" Padding="5" Click="Add_Variable"/>
            <Button Content="Добавить функцию" Grid.Row="1" Grid.Column="2" Margin="5" Padding="5" Click="Add_Function"/>
            <TextBlock x:Name="ResultText" Grid.Row="2" Grid.ColumnSpan="3" Margin="5" Padding="3" FontSize="16" FontWeight="Bold"/>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.5*"/>
                <RowDefinition Height="0.5*"/>
            </Grid.RowDefinitions>
            <ScrollViewer Margin="5" Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="Variables" Padding="3" FontSize="16" TextWrapping="Wrap"/>
            </ScrollViewer>
            <ScrollViewer Margin="5" Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="Functions" Padding="3" FontSize="16" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
